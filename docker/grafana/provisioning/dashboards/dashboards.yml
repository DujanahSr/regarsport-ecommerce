apiVersion: 1

providers:
  - name: 'RegarStore Dashboards'
    orgId: 1
    folder: 'RegarStore'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
